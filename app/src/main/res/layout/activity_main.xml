<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:weightSum="11"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android" >

    <include
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3"
        layout="@layout/header_information"/>

    <Button
        android:id="@+id/btn_create_new"
        style="@style/BtnCreateNew"
        android:paddingEnd="100dp"
        android:paddingLeft="110dp"
        android:drawableLeft="@drawable/ic_plus"
        android:textAllCaps="false"
        android:text="Tạo mới"
        android:layout_marginTop="10dp"
        android:background="@drawable/btn_login_selector"
        android:layout_marginLeft="@dimen/btn_margin_left"
        android:layout_width="@dimen/btn_width_activity_main"
        android:layout_height="50dp" />
    <TextView
        android:text="@string/title_history"
        style="@style/TxtHistory"
        android:layout_margin="@dimen/btn_margin_activity_main"
        android:paddingLeft="@dimen/btn_padding_left_activity_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rcv_history"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="8"
        >
    </androidx.recyclerview.widget.RecyclerView>

</LinearLayout>